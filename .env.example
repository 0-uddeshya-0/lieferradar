# Database
DATABASE_URL="postgresql://lieferradar:password@localhost:5432/lieferradar"

# JWT
JWT_SECRET="change-me-to-a-random-64-char-string-for-development-only"
JWT_EXPIRES_IN="15m"
REFRESH_TOKEN_EXPIRES_IN_DAYS="30"

# Email (SMTP)
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM_NAME="LieferRadar"
EMAIL_FROM_ADDRESS="noreply@lieferradar.local"

# App URLs
API_URL="http://localhost:3001"
WEB_URL="http://localhost:5173"
VITE_API_URL="http://localhost:3001"

# Environment
NODE_ENV="development"

# Cron
REMINDER_CRON="0 * * * *"
DIGEST_CRON="0 8 * * 1"
